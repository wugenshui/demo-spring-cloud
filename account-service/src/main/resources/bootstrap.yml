spring:
  application:
    name: account-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
ribbon:
  ConnectTimeout: 2000
  ReadTimeout: 5000
feign:
  sentinel:
    enabled: true